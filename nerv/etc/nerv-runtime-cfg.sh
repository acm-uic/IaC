#!/usr/bin/env sh

set -eux

# if test -f /etc/sssd/sssd.conf; then
#     rm /etc/sssd/sssd.conf
# fi

# mkdir -p /etc/sssd
# cp /usr/nerv/sssd/sssd.conf /etc/sssd/sssd.conf && echo "Replaced sssd.conf" || echo "Failed to replace sssd.conf"

# systemctl restart sssd
